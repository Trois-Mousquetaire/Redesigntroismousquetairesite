# AI testing rounds

One record file per round (`round-1.md`, `round-2.md`, ...) — the committed
record is the source of truth; the Discord `ai-testing` forum thread is its live
mirror. See `.claude/skills/ai-testing-live/SKILL.md` for the full flow, and
note the hard gate: no release until a human ✅ on the round.
